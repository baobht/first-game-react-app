import React, { useEffect, useState } from "react";

// import "./styles.scss";

function draw(_gameWidth: any, _gameHeight: any) {
  let gameWidth = _gameWidth;
  let gameHeight = _gameHeight;
  let states: any[] = [];
  let currentState = states[0];
  let image = document.getElementById("dog");
  let width = 200;
  let height = 181.83;
  let x = 0;
  let y = 0;

//   return context.drawImage(image, x, y);
}
export default draw;
